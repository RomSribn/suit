import { app } from './app'
import { routingStore as routing} from './routingStore';
import user from './user'
import garments from './garments'

export default {
  app,
  user,
  garments,
  routing,
}
